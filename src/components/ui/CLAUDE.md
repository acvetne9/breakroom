# src/components/ui/

The five shadcn/ui primitives the app actually uses: `skeleton`, `sonner` (toast host), `toast` + `toaster`, `tooltip`. The other ~40 generated primitives and their Radix packages were removed; add one back with `npx shadcn@latest add <name>` when a feature needs it.
