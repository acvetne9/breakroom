# Copy to android/keystore.properties (gitignored) and fill in.
# Paths are relative to the android/ directory. Generate a NEW keystore; the
# old one was committed to git history and must be treated as compromised.
KEYSTORE_FILE=../secrets/upload-key.jks
KEYSTORE_PASSWORD=
KEY_ALIAS=upload
KEY_PASSWORD=
